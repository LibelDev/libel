export const success = {
  import: '成功匯入 {{ users.length }} 個會員、{{ labels.length }} 個標籤、{{ subscriptions.length }} 個訂閱',
  export: '成功匯出 {{ users.length }} 個會員、{{ labels.length }} 個標籤、{{ subscriptions.length }} 個訂閱'
};
