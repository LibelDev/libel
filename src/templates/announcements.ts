import { displayName } from '../../package.json';

export const versionUpdate = {
  newVersionMessage: `《${displayName}》 已推出新版本 {{ newVersion }}`,
  oldVersionMessage: '你正在使用 {{ currentVersion }} 版本'
};
