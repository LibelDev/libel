import { displayName, homepage } from '../../package.json';

export const newVersion = `【${displayName}】 <a href="${homepage}/dist/libel.user.js" target="_blank">新版本<strong>{{newVersion}}</strong></a> 已推出（<a href="{{release.html_url}}" target="_blank">更新內容</a>）`;
